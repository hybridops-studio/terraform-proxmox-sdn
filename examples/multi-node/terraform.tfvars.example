# file: examples/multi-node/terraform.tfvars.example
# purpose: Example inputs for the multi-node SDN example

# Proxmox API configuration
proxmox_url      = "https://<PROXMOX-IP>:8006/api2/json"
# Token format example only. Use a dedicated Proxmox API token.
proxmox_token    = "terraform@pve!sdn=<YOUR-API-TOKEN-SECRET>"
proxmox_insecure = true

# Cluster-wide SDN membership
proxmox_nodes = ["<NODE1-NAME>", "<NODE2-NAME>", "<NODE3-NAME>"]

# One reachable cluster node for SSH-based SDN apply and status operations
proxmox_host = "<NODE1-IP>"
